% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-workflow.R
\name{run_tata}
\alias{run_tata}
\title{Run the full TATA workflow}
\usage{
run_tata(
  sce,
  dimred = "PCA",
  dims = NULL,
  time_col = "timepoint",
  use_time = TRUE,
  cluster_col = "cluster",
  k = 15,
  cluster_method = c("louvain", "leiden", "walktrap"),
  alpha = 1,
  beta = 1,
  direction_threshold = 0.1,
  prune_threshold = 0.01,
  time_weight_mode = c("directional_confidence", "ordered"),
  do_pseudotime = TRUE,
  root_cluster = NULL,
  refine_clusters = TRUE,
  refine_min_cells = NULL,
  refine_min_split_fraction = 0.2,
  do_branch_probs = TRUE,
  expected_branches = NULL,
  n_pcs_if_missing = 20,
  seed = 1
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment}.}

\item{dimred}{Reduced dimension to use for graph construction.}

\item{dims}{Optional subset of dimensions.}

\item{time_col}{Column containing experimental time labels.}

\item{use_time}{Logical indicating whether known experimental time should be
used during graph abstraction. Set \code{FALSE} to run a topology-only TATA
variant.}

\item{cluster_col}{Name of the column where inferred cluster labels will be
stored.}

\item{k}{Number of neighbours for the cell-cell kNN graph.}

\item{cluster_method}{Graph clustering method. Choices are:
\itemize{
\item \code{"louvain"}: fast modularity-based clustering and the default option.
\item \code{"leiden"}: Leiden community detection if available in the installed
\code{igraph}; otherwise TATA falls back to Louvain.
\item \code{"walktrap"}: random-walk clustering that can give slightly coarser states.
}}

\item{alpha}{Exponent for topology weights.}

\item{beta}{Exponent for time weights.}

\item{direction_threshold}{Threshold for direction assignment.}

\item{prune_threshold}{Minimum TATA weight to retain an abstract graph edge.}

\item{time_weight_mode}{How the time signal contributes to the final edge
score. Choices are:
\itemize{
\item \code{"directional_confidence"}: use direction strength regardless of whether
the label ordering is \code{a -> b} or \code{b -> a}; this is the recommended option
for multi-branch graphs.
\item \code{"ordered"}: use the original ordered-pair score \code{(flow_score + 1) / 2},
which is stricter but more sensitive to the arbitrary ordering of cluster
labels.
}}

\item{do_pseudotime}{Logical indicating whether to compute pseudotime.}

\item{root_cluster}{Optional root cluster for pseudotime.}

\item{refine_clusters}{Logical indicating whether to apply a conservative
temporal refinement step after graph clustering.}

\item{refine_min_cells}{Minimum cluster size considered for temporal
refinement.}

\item{refine_min_split_fraction}{Minimum fraction of cells required in both
temporal subclusters for a split to be accepted.}

\item{do_branch_probs}{Logical indicating whether to compute terminal-branch
probabilities and entropy/plasticity scores.}

\item{expected_branches}{Optional expected number of terminal branches. If
supplied, TATA uses it to guide terminal-state selection for downstream
branch probabilities and branch assignment.}

\item{n_pcs_if_missing}{Number of PCs to compute if \code{dimred} is missing.}

\item{seed}{Random seed.}
}
\value{
A list containing the updated \code{sce}, cell graph, adjacency matrix,
cluster graph, cluster edge table, cluster pseudotime, cell pseudotime, and
parameters used.
}
\description{
Run Time Aware Trajectory Analysis (TATA) end-to-end on a
\code{SingleCellExperiment}.
}
\examples{
sce <- simulate_tata_sce(n_cells = 500, n_features = 60, seed = 1)
tata <- run_tata(sce, dimred = "PCA", k = 12, seed = 1)
names(tata)
}
