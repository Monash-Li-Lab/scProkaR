% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-pseudobulk.R
\name{aggregate_pseudobulk}
\alias{aggregate_pseudobulk}
\title{Aggregate single-cell data into pseudobulk samples}
\usage{
aggregate_pseudobulk(
  sce,
  sample_cols,
  assay_name = "counts",
  aggregation = c("sum", "mean"),
  min_cells = 1L,
  add_logcounts = TRUE,
  prior_count = 2,
  sample_prefix = "pb_"
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment}.}

\item{sample_cols}{Character vector of \code{colData(sce)} columns that together
define each pseudobulk sample, for example \code{c("sample_id", "timepoint", "treatment", "cluster")}.}

\item{assay_name}{Assay containing the input counts.}

\item{aggregation}{Aggregation method. Choices are:
\itemize{
\item \code{"sum"}: sum counts across cells in each pseudobulk sample. This is the
recommended default for count-based edgeR workflows.
\item \code{"mean"}: average expression across cells in each pseudobulk sample. This
can be useful for exploratory summaries but is not ideal for count-based
NB models.
}}

\item{min_cells}{Minimum number of cells required for a pseudobulk sample to
be retained.}

\item{add_logcounts}{Logical indicating whether to add a \code{logcounts} assay
using \code{edgeR::cpm(..., log = TRUE)}.}

\item{prior_count}{Prior count passed to \code{edgeR::cpm()} when \code{add_logcounts = TRUE}.}

\item{sample_prefix}{Prefix for the generated pseudobulk column names.}
}
\value{
A pseudobulk \code{SingleCellExperiment} with one column per aggregated
sample. The output \code{colData} includes the grouping metadata, \code{ncells}, and
\code{lib.size}.
}
\description{
Summarize single-cell counts into pseudobulk samples defined by one or more
metadata columns. This is a common first step before sample-level
differential expression analysis with tools such as edgeR.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 600,
    n_features = 60,
    n_pcs = 10
)

# Split cells into three pseudo-replicates per condition/timepoint.
strata <- interaction(sce$condition, sce$timepoint, drop = TRUE)
rep_id <- integer(ncol(sce))
for (level_name in levels(strata)) {
    idx <- which(strata == level_name)
    rep_id[idx] <- sample(rep(seq_len(3), length.out = length(idx)))
}
sce$replicate <- factor(rep_id)

pb <- aggregate_pseudobulk(
    sce,
    sample_cols = c("condition", "timepoint", "replicate"),
    aggregation = "sum",
    min_cells = 5
)
pb
head(as.data.frame(SummarizedExperiment::colData(pb)))
}
