% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-edger.R
\name{run_scproka_de}
\alias{run_scproka_de}
\title{Run a full scProkaR differential expression workflow}
\usage{
run_scproka_de(
  sce,
  mode = c("pairwise", "time_series"),
  sample_cols,
  assay_name = "counts",
  aggregation = c("sum", "mean"),
  min_cells = 10L,
  normalization = c("TMM", "upperquartile", "none"),
  ...
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment}.}

\item{mode}{Workflow mode. Choices are:
\itemize{
\item \code{"pairwise"}: pseudobulk followed by pairwise edgeR contrasts.
\item \code{"time_series"}: pseudobulk followed by spline-based time-series edgeR.
}}

\item{sample_cols}{Metadata columns used to define pseudobulk samples.}

\item{assay_name}{Assay containing input counts.}

\item{aggregation}{Pseudobulk aggregation method. Choices are:
\itemize{
\item \code{"sum"}: sum counts within each pseudobulk sample.
\item \code{"mean"}: average counts within each pseudobulk sample.
}}

\item{min_cells}{Minimum number of cells required per pseudobulk sample.}

\item{normalization}{Pseudobulk normalization method. Choices are:
\itemize{
\item \code{"TMM"}: edgeR TMM normalization.
\item \code{"upperquartile"}: edgeR upper-quartile normalization.
\item \code{"none"}: no normalization factors beyond raw library sizes.
}}

\item{...}{Additional arguments passed to \code{run_edger_pairwise_de()} or
\code{run_edger_spline_de()}, depending on \code{mode}.}
}
\value{
A list with the pseudobulk object, the DE result, and workflow
settings.
}
\description{
Aggregate single-cell counts into pseudobulk samples and then dispatch to a
pairwise or time-series edgeR workflow.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 600, n_features = 60, n_pcs = 10
)
sce$replicate <- rep(paste0("rep", 1:3), length.out = ncol(sce))

# Pseudobulk aggregation plus pairwise edgeR contrasts in one call.
pairwise_res <- run_scproka_de(
    sce,
    mode = "pairwise",
    sample_cols = c("condition", "timepoint", "replicate"),
    min_cells = 5,
    group_col = "timepoint",
    covariates = "condition"
)
dim(pairwise_res$pseudobulk)
names(pairwise_res$de_result$tables)

# The same pseudobulk design analysed as a spline time series.
ts_res <- run_scproka_de(
    sce,
    mode = "time_series",
    sample_cols = c("condition", "timepoint", "replicate"),
    min_cells = 5,
    time_col = "timepoint",
    condition_col = "condition",
    curve_grid_length = 50
)
head(ts_res$de_result$combined_table[, c("gene", "FDR", "condition")])
}
