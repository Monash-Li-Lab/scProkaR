% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory.R
\name{RunTrajectory}
\alias{RunTrajectory}
\title{Run PAGA-inspired and time/dose trajectory analysis}
\usage{
RunTrajectory(
  object,
  reduction = c("fdl", "X_fdl", "umap", "UMAP", "pca", "PCA"),
  cluster_col = "seurat_clusters",
  time_col = NULL,
  dose_col = NULL,
  trajectory_col = NULL,
  real_value = "real",
  n_neighbors = 15,
  edge_threshold = 0.1,
  valid_times = NULL,
  valid_doses = NULL,
  baseline_dose = 0,
  baseline_time = 0,
  density_floor = 0.2,
  kde_n = 100,
  min_cells_density = 10,
  seed = 1,
  verbose = TRUE,
  dims = NULL
)
}
\arguments{
\item{object}{A \code{SingleCellExperiment}.}

\item{reduction}{Candidate reduced-dimension names to use directly as the
neighbour and plotting space. Existing embeddings are preferred and no PCA
is recomputed when the reduction exists.}

\item{cluster_col}{Metadata column containing existing cluster labels.}

\item{time_col}{Optional metadata column containing time.}

\item{dose_col}{Optional metadata column containing dose.}

\item{trajectory_col}{Optional metadata column used to restrict analysis to
real cells.}

\item{real_value}{Value in \code{trajectory_col} indicating real cells.}

\item{n_neighbors}{Number of neighbours used to build the cell-level graph.}

\item{edge_threshold}{Minimum connectivity weight to emphasize in plots.}

\item{valid_times}{Optional explicit ordered time values.}

\item{valid_doses}{Optional explicit ordered dose values.}

\item{baseline_dose}{Baseline dose value used for shared-baseline panels.}

\item{baseline_time}{Baseline time value used for shared-baseline panels.}

\item{density_floor}{Lower clamp used by density plots.}

\item{kde_n}{Grid size passed to \code{MASS::kde2d()} when available.}

\item{min_cells_density}{Minimum panel size before using a constant fallback
density.}

\item{seed}{Random seed used for reproducible helper operations.}

\item{verbose}{Whether to emit warnings for skipped panels and fallbacks.}

\item{dims}{Optional dimensions of the embedding to use. When \code{NULL}, all
available dimensions are used for neighbours and the first two are used for
plotting.}
}
\value{
A \code{SingleCellExperiment} with trajectory results stored in metadata.
}
\description{
\code{RunTrajectory()} performs two linked analyses on an existing embedding:
}
\details{
\enumerate{
\item A PAGA-inspired cluster abstraction that builds a cell-level kNN graph in
the supplied neighbour space and summarizes cluster-cluster connectivity.
\item A Squidiff-style downstream trajectory summary that works on real cells,
computes per-panel density on the same embedding, tracks time-ordered
centroids, and summarizes cluster composition over time and optionally
dose.
}

The graph abstraction is intentionally PAGA-inspired rather than an exact
Scanpy reimplementation. Cluster connectivity is normalized as
\code{observed_edges / min(k * n_i, k * n_j)} and therefore bounded in \verb{[0, 1]}.

When both \code{time_col} and \code{dose_col} are supplied, dose-specific trajectory
panels use the following baseline rule:
\itemize{
\item for \code{dose == baseline_dose}, use cells from that dose at all valid times
\item for \code{dose > baseline_dose}, use shared baseline cells at
\verb{(baseline_dose, baseline_time)} plus cells from the target dose at
non-baseline timepoints
}

Results are stored in \code{metadata(object)$SCProkaR$trajectory}, with
\code{paga}, \code{density}, \code{composition}, and \code{params} entries.
}
