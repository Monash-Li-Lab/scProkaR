% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de-edger.R
\name{run_edger_pairwise_de}
\alias{run_edger_pairwise_de}
\title{Run pairwise pseudobulk differential expression with edgeR}
\usage{
run_edger_pairwise_de(
  pb,
  assay_name = "counts",
  group_col = "timepoint",
  covariates = NULL,
  combine_group_cols = NULL,
  contrast_type = c("reference", "all", "manual"),
  reference_level = NULL,
  manual_pairs = NULL,
  normalization = c("TMM", "upperquartile", "none"),
  filter_by_expr = TRUE,
  robust = TRUE,
  abundance_trend = FALSE,
  fdr_cutoff = 0.05
)
}
\arguments{
\item{pb}{A pseudobulk \code{SingleCellExperiment}.}

\item{assay_name}{Assay containing pseudobulk counts.}

\item{group_col}{Sample metadata column defining the primary grouping, for
example \code{"timepoint"}.}

\item{covariates}{Optional metadata columns to include as additive covariates
in the design matrix, for example treatment, batch, or donor.}

\item{combine_group_cols}{Optional metadata columns to combine into a single
composite group via \code{interaction()}, for example
\code{c("treatment", "timepoint")}.}

\item{contrast_type}{Contrast construction strategy. Choices are:
\itemize{
\item \code{"reference"}: compare every non-reference group against one reference
level. This is the default.
\item \code{"all"}: test all pairwise group contrasts.
\item \code{"manual"}: use \code{manual_pairs} exactly as supplied.
}}

\item{reference_level}{Optional reference level used when
\code{contrast_type = "reference"}. By default the earliest or first group is
used.}

\item{manual_pairs}{Optional two-column matrix or data frame of manual
contrasts with \code{group1} and \code{group2} values. Each test is \code{group1 - group2}.}

\item{normalization}{Library-size normalization method. Choices are:
\itemize{
\item \code{"TMM"}: edgeR TMM normalization.
\item \code{"upperquartile"}: edgeR upper-quartile normalization.
\item \code{"none"}: no normalization factors beyond raw library sizes.
}}

\item{filter_by_expr}{Logical indicating whether to run
\code{edgeR::filterByExpr()}.}

\item{robust}{Logical passed to \code{edgeR::glmQLFit()}.}

\item{abundance_trend}{Logical passed to \code{edgeR::glmQLFit()}.}

\item{fdr_cutoff}{FDR cutoff used to mark significant genes.}
}
\value{
A list containing the fitted edgeR objects, contrast matrix, result
tables, and analysis settings.
}
\description{
Fit a quasi-likelihood negative-binomial model with edgeR and test pairwise
contrasts between groups such as timepoints, treatments, or composite
treatment-timepoint groups.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 600, n_features = 60, n_pcs = 10
)
sce$replicate <- rep(paste0("rep", 1:3), length.out = ncol(sce))

pb <- aggregate_pseudobulk(
    sce,
    sample_cols = c("condition", "timepoint", "replicate"),
    aggregation = "sum",
    min_cells = 5
)
pb <- normalize_pseudobulk(pb, method = "TMM")

# Compare every timepoint against the baseline, adjusting for drug.
de <- run_edger_pairwise_de(
    pb,
    group_col = "timepoint",
    covariates = "condition",
    contrast_type = "reference"
)
names(de$tables)
head(de$tables[["24_vs_0"]][, c("gene", "logFC", "FDR", "significant")])
table(de$combined_table$contrast, de$combined_table$significant)
}
