% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{RunIntegratedClustering}
\alias{RunIntegratedClustering}
\title{Cluster cells on an integrated embedding}
\usage{
RunIntegratedClustering(
  sce,
  reduction = NULL,
  cluster_col = "integrated_clusters",
  dims = NULL,
  k = 20,
  algorithm = c("louvain", "walktrap", "leiden"),
  resolution = 0.8
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment}.}

\item{reduction}{Integrated reduced-dimension name. If \code{NULL}, the function
prefers a stored \verb{integrated_*} embedding.}

\item{cluster_col}{Column name used to store cluster labels in
\code{colData(sce)}.}

\item{dims}{Optional dimensions to retain from the embedding.}

\item{k}{Number of nearest neighbours used to build the graph.}

\item{algorithm}{Graph clustering algorithm: \code{"louvain"} or \code{"walktrap"}.}

\item{resolution}{Cluster granularity parameter. This is analogous to
Seurat's \code{resolution}: higher values typically produce more clusters. It is
used when the selected graph clustering algorithm supports it.}
}
\value{
A \code{SingleCellExperiment} with cluster labels added to \code{colData(sce)}.
}
\description{
Builds a k-nearest-neighbour graph on a corrected embedding and detects
communities for downstream trajectory and pseudobulk workflows.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 300, n_features = 60, n_pcs = 10
)
sce <- RunIntegratedClustering(
    sce,
    reduction = "PCA",
    cluster_col = "pca_clusters",
    dims = 1:8,
    k = 15,
    algorithm = "louvain",
    resolution = 0.8
)
print(table(sce$pca_clusters))
print(table(sce$pca_clusters, sce$condition))
}
