% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-plot.R
\name{plot_tata_cluster_graph}
\alias{plot_tata_cluster_graph}
\title{Plot the cluster-level TATA graph}
\usage{
plot_tata_cluster_graph(
  tata_result,
  dimred = NULL,
  layout_mode = NULL,
  cluster_col = NULL,
  show_cells = FALSE,
  colour_by = NULL,
  node_colour_by = c("median_time", "cluster"),
  main = "TATA cluster graph",
  vertex_label_size = 3.5,
  edge_width_scale = 5,
  point_size = 0.4,
  point_alpha = 0.45,
  arrow_size = 0.15
)
}
\arguments{
\item{tata_result}{Result list returned by \code{run_tata()}.}

\item{dimred}{Optional reduced dimension used to position cluster nodes. When
supplied together with \code{layout_mode = "embedding"}, cluster centroids are
computed from this embedding.}

\item{layout_mode}{Layout mode for the graph. Choices are:
\itemize{
\item \code{"graph"}: use the stored abstract graph coordinates, or a force-directed
layout if they are unavailable.
\item \code{"embedding"}: place cluster nodes at their centroids in the selected
reduced dimension.
}}

\item{cluster_col}{Optional cluster column used when \code{layout_mode = "embedding"}. By default this is taken from \code{tata_result$parameters}.}

\item{show_cells}{Logical indicating whether to draw cells in the background
when \code{layout_mode = "embedding"}.}

\item{colour_by}{Optional \code{colData} column used to color background cells. If
\code{NULL} and \code{show_cells = TRUE}, the cluster column is used when available.}

\item{node_colour_by}{Node coloring scheme. Choices are:
\itemize{
\item \code{"median_time"}: color nodes by cluster median time.
\item \code{"cluster"}: color nodes by cluster identity, which is useful for
embedding-aligned graph plots.
}}

\item{main}{Plot title.}

\item{vertex_label_size}{Label size.}

\item{edge_width_scale}{Relative edge width scale.}

\item{point_size}{Background point size when cells are drawn.}

\item{point_alpha}{Background point alpha when cells are drawn.}

\item{arrow_size}{Arrow size for directed edges.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot the directed cluster-level TATA graph. The graph can be drawn in its
own abstract layout or positioned on a chosen reduced dimension so that the
graph coordinates match the embedding shown to the user.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 600, n_features = 60,
    n_pcs = 10
)
tata <- run_tata(sce,
    dimred = "PCA", time_col = "timepoint", k = 15,
    expected_branches = 3
)

# Abstract graph layout, nodes coloured by cluster median time.
p <- plot_tata_cluster_graph(tata)

# The same graph positioned on the UMAP embedding.
p_embedded <- plot_tata_cluster_graph(tata,
    dimred = "UMAP",
    layout_mode = "embedding", show_cells = TRUE,
    node_colour_by = "cluster"
)
p_embedded
}
