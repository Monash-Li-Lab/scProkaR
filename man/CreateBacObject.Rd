% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_object.R
\name{CreateBacObject}
\alias{CreateBacObject}
\title{Create a standardized bacterial single-cell object}
\usage{
CreateBacObject(
  x,
  counts_assay = "counts",
  cell_metadata = NULL,
  feature_metadata = NULL,
  sample_col = NULL,
  batch_col = NULL,
  condition_col = NULL,
  time_col = NULL,
  organism = "bacteria",
  seurat_assay = NULL,
  seurat_layer = "counts",
  transfer_reductions = TRUE
)
}
\arguments{
\item{x}{A gene-by-cell count matrix, a \code{SingleCellExperiment}, or a loaded
Seurat object from an \code{.rds} file.}

\item{counts_assay}{Assay name to use when \code{x} is a \code{SingleCellExperiment}.}

\item{cell_metadata}{Optional cell-level metadata with one row per cell.}

\item{feature_metadata}{Optional feature-level metadata with one row per gene.}

\item{sample_col}{Optional sample identifier column in \code{cell_metadata}.}

\item{batch_col}{Optional batch identifier column in \code{cell_metadata}.}

\item{condition_col}{Optional condition column in \code{cell_metadata}.}

\item{time_col}{Optional time or ordering column in \code{cell_metadata}.}

\item{organism}{Organism label stored in package metadata.}

\item{seurat_assay}{Assay name to extract when \code{x} is a Seurat object. If
\code{NULL}, the active/default Seurat assay is used.}

\item{seurat_layer}{Layer to use as raw counts when \code{x} is a Seurat object.
Defaults to \code{"counts"}.}

\item{transfer_reductions}{Logical indicating whether existing Seurat
dimensional reductions (for example \code{pca} and \code{umap}) should be copied into
\code{reducedDims(sce)}.}
}
\value{
A standardized \code{SingleCellExperiment}.
}
\description{
\code{CreateBacObject()} standardizes raw microbial single-cell counts into a
\code{SingleCellExperiment}, aligns cell and feature metadata, and records package
provenance used by downstream SCProkaR workflows.
}
