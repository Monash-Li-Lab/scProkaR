% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-plot.R
\name{plot_tata_gene_trend_heatmap}
\alias{plot_tata_gene_trend_heatmap}
\title{Plot a heatmap of TATA gene trends}
\usage{
plot_tata_gene_trend_heatmap(
  trend_result,
  branches = NULL,
  scale_rows = TRUE,
  n_clusters = NULL
)
}
\arguments{
\item{trend_result}{Result list returned by \code{compute_tata_gene_trends()}.}

\item{branches}{Optional subset of branches.}

\item{scale_rows}{Logical indicating whether to z-score each gene-branch
trend before plotting.}

\item{n_clusters}{Optional number of k-means clusters used to order genes by
trend shape.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot a heatmap of branch-specific fitted gene-expression trends over TATA
pseudotime.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 600, n_features = 60,
    n_pcs = 10
)
tata <- run_tata(sce,
    dimred = "PCA", time_col = "timepoint", k = 15,
    expected_branches = 3
)

# Z-scored trends, with genes grouped by k-means on trend shape.
trends <- compute_tata_gene_trends(tata,
    genes = head(rownames(tata$sce), 12)
)
p <- plot_tata_gene_trend_heatmap(trends,
    scale_rows = TRUE,
    n_clusters = 3
)
p
}
