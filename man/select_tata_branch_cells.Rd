% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-branch.R
\name{select_tata_branch_cells}
\alias{select_tata_branch_cells}
\title{Select cells from a TATA branch}
\usage{
select_tata_branch_cells(
  tata_result,
  branch,
  selection_mode = c("soft", "hard"),
  probability_threshold = 0.25
)
}
\arguments{
\item{tata_result}{Result list returned by \code{run_tata()}.}

\item{branch}{Terminal branch or terminal cluster to select.}

\item{selection_mode}{How to define selected cells. Choices are:
\itemize{
\item \code{"soft"}: select cells whose probability for the branch is at least
\code{probability_threshold}.
\item \code{"hard"}: select cells whose hard TATA branch assignment equals \code{branch}.
}}

\item{probability_threshold}{Minimum branch probability required for
\code{selection_mode = "soft"}.}
}
\value{
A data frame with cell ids, branch probabilities, and a logical
\code{selected} column.
}
\description{
Select cells that belong to a chosen terminal branch using either hard branch
assignment or a probability threshold.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 600, n_features = 60, n_pcs = 10
)
tata <- run_tata(sce, dimred = "PCA", time_col = "timepoint", k = 15,
    expected_branches = 3)
branch <- tata$terminal_clusters[1]
soft <- select_tata_branch_cells(tata, branch = branch,
    selection_mode = "soft", probability_threshold = 0.3)
head(soft)
table(soft$selected)
hard <- select_tata_branch_cells(tata, branch = branch,
    selection_mode = "hard")
table(hard$selected)
}
