% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{FilterBacCells}
\alias{FilterBacCells}
\title{Filter cells using bacterial QC thresholds}
\usage{
FilterBacCells(
  sce,
  min_counts = NULL,
  min_features = NULL,
  max_rrna_fraction = NULL,
  custom_filter = NULL
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment} with QC metrics already present.}

\item{min_counts}{Minimum allowed total counts.}

\item{min_features}{Minimum allowed number of detected features.}

\item{max_rrna_fraction}{Maximum allowed rRNA fraction.}

\item{custom_filter}{Optional custom filter. May be a logical vector, a
column name in \code{colData(sce)}, or a function returning a logical vector.}
}
\value{
A filtered \code{SingleCellExperiment}.
}
\description{
Applies simple threshold-based filtering to QC metrics produced by
\code{RunBacQC()}.
}
\examples{
set.seed(1)
genes <- c("rrsA", "rrlB", "rplA", "rpsB", paste0("gene", 1:26))
counts <- matrix(
    rpois(length(genes) * 50, lambda = 3),
    nrow = length(genes),
    dimnames = list(genes, paste0("cell", seq_len(50)))
)
sce <- SingleCellExperiment::SingleCellExperiment(
    assays = list(counts = counts)
)
sce <- RunBacQC(sce)

filtered <- FilterBacCells(
    sce,
    min_counts = 80,
    min_features = 20,
    max_rrna_fraction = 0.2
)
c(before = ncol(sce), after = ncol(filtered))

# A custom filter may be given as a function of colData()
custom <- FilterBacCells(
    sce,
    custom_filter = function(cd) {
        cd$detected_features >= stats::median(cd$detected_features)
    }
)
ncol(custom)
}
