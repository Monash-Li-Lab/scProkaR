% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-plot.R
\name{plot_tata_branch_selection}
\alias{plot_tata_branch_selection}
\title{Plot selected cells from one TATA branch}
\usage{
plot_tata_branch_selection(
  tata_result,
  branch,
  dimred = "UMAP",
  selection_mode = c("soft", "hard"),
  probability_threshold = 0.25,
  point_size = 0.6,
  point_alpha = 0.8
)
}
\arguments{
\item{tata_result}{Result list returned by \code{run_tata()}.}

\item{branch}{Terminal branch or terminal cluster to select.}

\item{dimred}{Reduced dimension to plot.}

\item{selection_mode}{How to define selected cells. Choices are:
\itemize{
\item \code{"soft"}: select cells whose probability for the branch is at least
\code{probability_threshold}.
\item \code{"hard"}: select cells whose hard TATA branch assignment equals \code{branch}.
}}

\item{probability_threshold}{Minimum branch probability required for
\code{selection_mode = "soft"}.}

\item{point_size}{Point size.}

\item{point_alpha}{Point alpha.}
}
\value{
A \code{ggplot2} object.
}
\description{
Highlight cells that belong to one terminal branch using either a hard or a
soft branch-selection rule.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(n_cells = 600, n_features = 60,
    n_pcs = 10)
tata <- run_tata(sce, dimred = "PCA", time_col = "timepoint", k = 15,
    expected_branches = 3)

# Highlight cells with an appreciable probability of one terminal fate.
branch <- tata$terminal_clusters[1]
p <- plot_tata_branch_selection(tata, branch = branch, dimred = "UMAP",
    selection_mode = "soft", probability_threshold = 0.25)
p
}
