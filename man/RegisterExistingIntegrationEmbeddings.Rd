% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{RegisterExistingIntegrationEmbeddings}
\alias{RegisterExistingIntegrationEmbeddings}
\title{Register existing reduced dimensions as integration embeddings}
\usage{
RegisterExistingIntegrationEmbeddings(
  sce,
  reductions = NULL,
  method_names = NULL,
  copy = TRUE,
  prefix = "integrated_",
  metadata = list(),
  overwrite = FALSE
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment}.}

\item{reductions}{Character vector of existing reduced-dimension names.
If \code{NULL}, likely integration reductions are detected from their names.
Pass names explicitly when automatic detection is unsuitable.}

\item{method_names}{Optional names to use in benchmark tables. Must be
unique and have the same length as \code{reductions}. By default, names are
inferred from the reductions and converted to lower case.}

\item{copy}{Logical. If \code{TRUE}, reductions not already starting with
\code{prefix} are copied to \code{paste0(prefix, method_name)}. If \code{FALSE}, their
original names are registered directly.}

\item{prefix}{Prefix used when \code{copy = TRUE}.}

\item{metadata}{Optional named list added to each method's metadata. A list
entry named by \code{method_name}, if present, is used for that method instead.
\code{source} and \code{original_reduction} are recorded by this function.}

\item{overwrite}{Logical indicating whether copied reduced dimensions may
replace existing reductions with different values.}
}
\value{
A \code{SingleCellExperiment} with registered integration embeddings.
}
\description{
Registers embeddings already stored in a \code{SingleCellExperiment}, including
Seurat CCA/RPCA, Scanorama, scVI, SCALEX, BBKNN, ComBat and LIGER latent
spaces. It does not run these external tools or import their native objects.
The selected reductions are copied or referenced and recorded in
\code{metadata(sce)$scProkaR$integration}, so \code{\link[=BenchmarkIntegration]{BenchmarkIntegration()}} can compare
them with scProkaR MNN and Harmony outputs.
}
\examples{
set.seed(1)
sce <- simulate_tata_sce(n_cells = 1000, n_features = 60)
# Stand-in for a latent space from an external integration tool.
SingleCellExperiment::reducedDim(sce, "X_scVI") <-
    SingleCellExperiment::reducedDim(sce, "PCA")[, seq_len(3)]
sce <- RegisterExistingIntegrationEmbeddings(sce, reductions = "X_scVI")
SingleCellExperiment::reducedDimNames(sce)
S4Vectors::metadata(sce)$scProkaR$integration$results$scvi
}
