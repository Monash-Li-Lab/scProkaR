% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{IntegrateBacData}
\alias{IntegrateBacData}
\title{Integrate microbial single-cell batches}
\usage{
IntegrateBacData(
  sce,
  batch_col,
  method = c("mnn", "harmony"),
  feature_set = c("hvg", "all"),
  dims = 1:30,
  assay_name = "counts",
  integrated_name = NULL,
  ...
)
}
\arguments{
\item{sce}{A \code{SingleCellExperiment}.}

\item{batch_col}{Column in \code{colData(sce)} containing batch assignments.}

\item{method}{Integration backend: \code{"mnn"} or \code{"harmony"}.}

\item{feature_set}{Either \code{"hvg"} or \code{"all"}.}

\item{dims}{Integer vector of dimensions to retain.}

\item{assay_name}{Assay used for preprocessing.}

\item{integrated_name}{Optional name for the output reduced dimension.}

\item{...}{Additional backend-specific arguments.}
}
\value{
A \code{SingleCellExperiment} with an integrated reduced dimension.
}
\description{
Runs one of several integration backends and stores the corrected embedding
in \code{reducedDims(sce)}.
}
\examples{
set.seed(1)
sce <- simulate_tata_multidrug_sce(
    n_cells = 300, n_features = 60, n_pcs = 10
)
# Assign a technical batch label to integrate over.
sce$batch <- rep(c("batch1", "batch2"), length.out = ncol(sce))

if (requireNamespace("batchelor", quietly = TRUE)) {
    sce <- IntegrateBacData(
        sce,
        batch_col = "batch",
        method = "mnn",
        dims = 1:8
    )
    print(SingleCellExperiment::reducedDimNames(sce))
}

if (requireNamespace("harmony", quietly = TRUE)) {
    sce <- IntegrateBacData(
        sce,
        batch_col = "batch",
        method = "harmony",
        dims = 1:8
    )
    harmony_emb <- SingleCellExperiment::reducedDim(
        sce, "integrated_harmony"
    )
    print(dim(harmony_emb))
}
}
