% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-simulate.R
\name{simulate_tata_sce}
\alias{simulate_tata_sce}
\alias{simulate_tata_divergent_sce}
\title{Simulate a divergent TATA dataset}
\usage{
simulate_tata_sce(n_cells = 5000, n_features = 100, n_pcs = 20, seed = 1)

simulate_tata_divergent_sce(
  n_cells = 5000,
  n_features = 100,
  n_pcs = 20,
  seed = 1
)
}
\arguments{
\item{n_cells}{Number of cells to simulate.}

\item{n_features}{Number of features to simulate.}

\item{n_pcs}{Number of principal components to store.}

\item{seed}{Random seed.}
}
\value{
A \code{SingleCellExperiment}.
}
\description{
Simulate a \code{SingleCellExperiment} containing single-bacterium observations on
a divergent two-way trajectory. Time \code{0} is centred at the origin of the
latent trajectory, and later timepoints move away from that central root into
two branches. This makes the simulated dataset useful for demonstrating TATA
on a divergent trajectory where the earliest state appears in the middle of a
UMAP.
}
\details{
The returned object contains:
\itemize{
\item \code{counts} and \code{logcounts}
\item \code{colData} with \code{cell_id}, \code{timepoint}, \code{condition}, \code{simulated_state}, and
\code{simulated_branch}
\item \code{reducedDims(sce)$PCA}
\item \code{reducedDims(sce)$UMAP}
\item \code{reducedDims(sce)$truth}
}
}
\examples{
sce <- simulate_tata_sce(n_cells = 500, n_features = 60, seed = 1)
dim(sce)
}
