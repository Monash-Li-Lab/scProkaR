% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-edge-weights.R
\name{build_tata_graph}
\alias{build_tata_graph}
\title{Build the TATA cluster graph}
\usage{
build_tata_graph(
  topology_table,
  time_table,
  clusters,
  embedding,
  timepoint,
  alpha = 1,
  beta = 1,
  direction_threshold = 0.1,
  prune_threshold = 0.01,
  time_weight_mode = c("directional_confidence", "ordered")
)
}
\arguments{
\item{topology_table}{Output from \code{compute_topology_weights()}.}

\item{time_table}{Output from \code{compute_time_weights()}.}

\item{clusters}{Cluster labels for cells.}

\item{embedding}{Low-dimensional embedding used to compute cluster centroids.}

\item{timepoint}{Experimental time labels.}

\item{alpha}{Exponent for topology weights.}

\item{beta}{Exponent for time weights.}

\item{direction_threshold}{Threshold for assigning edge direction.}

\item{prune_threshold}{Minimum TATA weight to retain an edge.}

\item{time_weight_mode}{How to incorporate time into the final edge weight.
Choices are:
\itemize{
\item \code{"directional_confidence"}: reward strong directional evidence regardless
of whether the supported direction is \code{a -> b} or \code{b -> a}.
\item \code{"ordered"}: use the original ordered-pair formulation
\code{(flow_score + 1) / 2}, which is stricter but depends on the ordering of
\code{cluster_a} and \code{cluster_b}.
}}
}
\value{
A list containing the directed cluster graph, the cluster edge table,
and cluster vertex metadata.
}
\description{
Combine topology and temporal consistency into final TATA edge weights, assign
edge direction, and prune weak edges to obtain the cluster-level TATA graph.
}
\examples{
set.seed(1)
sce <- simulate_tata_sce(n_cells = 1000, n_features = 60)
knn <- build_knn_graph(sce, dimred = "PCA", k = 12)
clusters <- cluster_graph_states(knn$graph, method = "louvain")
timepoint <- SummarizedExperiment::colData(sce)$timepoint
topology_table <- compute_topology_weights(knn$adjacency, clusters)
time_table <- compute_time_weights(knn$adjacency, clusters, timepoint)
tata_graph <- build_tata_graph(
    topology_table = topology_table,
    time_table = time_table,
    clusters = clusters,
    embedding = knn$embedding,
    timepoint = timepoint
)
igraph::ecount(tata_graph$graph)
head(tata_graph$vertex_table)
}
