% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tata-simulate.R
\name{simulate_tata_multidrug_sce}
\alias{simulate_tata_multidrug_sce}
\title{Simulate a multi-drug branching TATA dataset}
\usage{
simulate_tata_multidrug_sce(n_cells = 5000, n_features = 100, n_pcs = 20)
}
\arguments{
\item{n_cells}{Number of cells to simulate.}

\item{n_features}{Number of features to simulate.}

\item{n_pcs}{Number of principal components to store.}
}
\value{
A \code{SingleCellExperiment}.
}
\description{
Simulate a \code{SingleCellExperiment} with one shared origin trajectory that can
diverge into three drug-associated fates. The three drug-associated branches
emerge at different real treatment times, making the dataset useful for
checking whether TATA uses time information rather than feature similarity
alone when orienting cluster-level transitions.
}
\details{
The simulated design uses treatment times in hours:
\itemize{
\item \code{drug_A} branch emerges early, around 24 hours
\item \code{drug_C} branch emerges around 48 hours
\item \code{drug_B} branch emerges late, around 96 hours
}

The geometry is intentionally center-rooted rather than tip-rooted:
\code{drug_A} and \code{drug_B} extend in opposite directions from a shared middle
state, and \code{drug_C} diverges along a third arm. This is meant to stress-test
whether time labels can recover direction when the root is not visually
obvious from topology alone.
}
\examples{
sce <- simulate_tata_multidrug_sce(n_cells = 1000, n_features = 80)
table(sce$condition, sce$timepoint)
}
